These files are usually stored in on the SPIFFS setion on the ESP device. They are pointless since I couldn't get SPIFFS to work.

The `user_definitiions.json` file is used by 'nRF Connect' Android app.
